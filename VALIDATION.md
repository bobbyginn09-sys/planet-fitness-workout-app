# NEXSET 3.1.6 Validation

- JavaScript syntax checked with Node.
- Service worker syntax checked with Node.
- Manifest and service-worker asset references verified.
- Existing storage keys preserved: `pfWorkoutApp.v1` and `pfWorkoutApp.active.v1`.
- Header rendered as the N mark followed by `EXSET`; no duplicate N.
- Legs A, Legs B, and Active Recovery cards rendered at a 390 × 844 mobile viewport.
- Mark Rest Day flow rendered and verified without clipping or transformed fixed-position overflow.
